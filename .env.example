# ==================================================
# mccastellazzob.com - Ambiente di SVILUPPO Docker
# ==================================================
# Questo file è SOLO per lo sviluppo locale con Docker.
# In produzione si usano settings diversi (prod.py).
#
# Copia questo file in .env e modifica i valori

# Django Settings
# ---------------
DEBUG=True
SECRET_KEY=changeme_generate_a_real_secret_key_using_python_secrets_module
ALLOWED_HOSTS=localhost,127.0.0.1,mccastellazzob.com,www.mccastellazzob.com
WAGTAILADMIN_BASE_URL=http://localhost

# Database PostgreSQL
# -------------------
POSTGRES_DB=mccastellazzob
POSTGRES_USER=mccastellazzob
POSTGRES_PASSWORD=changeme_use_strong_password_in_production
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Superuser automatico (solo per sviluppo!)
# -----------------------------------------
CREATE_SUPERUSER=true
SUPERUSER_USERNAME=admin
SUPERUSER_EMAIL=admin@mccastellazzob.com
SUPERUSER_PASSWORD=admin123

# Email (configurare per produzione)
# ----------------------------------
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=True

# Logging
# -------
DJANGO_LOG_LEVEL=INFO
